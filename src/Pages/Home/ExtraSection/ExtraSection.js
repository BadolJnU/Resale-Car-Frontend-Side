import React from 'react';
import extraPic from '../../../assets/about.webp'

const ExtraSection = () => {
    return (
        <div className="hero min-h-screen bg-base-200">
  <div className="hero-content flex-col lg:flex-row">
    <img src={extraPic} alt='' className="max-w-sm rounded-lg shadow-2xl mx-10" />
    <div>
      <h1 className="text-5xl font-bold">Shop smart with AutoRadar</h1>
      <p className="py-6">They provide pleasure in desire and in. He sought to escape, as if he were to be expected to receive training. In the softening of them, or of repudiating them, and from that unless</p>
      <button className="btn btn-primary">Get Started</button>
    </div>
  </div>
</div>
    );
};

export default ExtraSection;